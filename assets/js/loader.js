window.addEventListener("load", function () {
    const loader = document.querySelector(".loader");
    loader.className += " hidden"; // class "loader hidden"

    const gallery= document.querySelector(".gallery");
    gallery.className += " aparece";
});